"""Subject records and discovery eligibility (step 8).

The one fact this service holds about a person beyond their ``user_ref``:
whether discovery may run for them at all. See :mod:`.eligibility` for why
that decision is ours rather than the proxy's.
"""
